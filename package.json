{
  "name": "marketgame-click",
  "private": true,
  "description": "Market Game: Capture the Market — web version for the U.S. market",
  "type": "module",
  "scripts": {
    "build:engine": "esbuild supabase/functions/game/economy.ts supabase/functions/game/presets.ts --format=esm --outdir=build --out-extension:.js=.mjs --log-level=warning",
    "test": "npm run build:engine && node tests/golden.mjs && node tests/scale_check.mjs && node tests/v5_check.mjs && node tests/reset_check.mjs",
    "report:teams": "npm run build:engine && node tests/teams_check.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
